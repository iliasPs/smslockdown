<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ip.smslockdown1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name="com.ip.smslockdown1.activities.AlertDialogActivity"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.AppCompat.Dialog">


        </activity>

        <receiver android:name="com.ip.smslockdown1.widget.SmsWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sms_widget_info" />
        </receiver>

        <activity
            android:name="com.ip.smslockdown1.activities.MainActivity"
            android:configChanges="locale|orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.ip.smslockdown1.activities.UserInputActivity"
            android:configChanges="locale|orientation"
            android:parentActivityName="com.ip.smslockdown1.activities.MainActivity"
            android:screenOrientation="portrait" />

        <receiver android:name="com.ip.smslockdown1.widget.SmsWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sms_widget_info" />
        </receiver>

    </application>

</manifest>